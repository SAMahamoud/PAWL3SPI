{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editer un Genre</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('genre') }}">
            Retour à la liste
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
{% use 'BlBibliothequeBundle:Admin:sidebar_admin.html.twig' %}
